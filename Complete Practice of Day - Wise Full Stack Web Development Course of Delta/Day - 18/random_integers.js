// generate some random integers

let num = Math.random();

num = num * 5; // we are going to generate the numbers between 1 to 5

num = Math.ceil(num); // we are reducing the extra points after the decimal

console.log(num);
