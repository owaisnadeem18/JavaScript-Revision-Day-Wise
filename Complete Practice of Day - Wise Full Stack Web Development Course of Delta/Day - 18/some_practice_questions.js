// Generate some random numbers between 1 to 100 (must use floor)

// let num = Math.random();

// num = Math.floor(num * 100) + 1; // we are ranging it between 1 to 100

// console.log(num);

// Generate some random numbers between 1 to 5 (must use floor)

// let num = Math.random();

//generate them between 01 to 05

// num = Math.floor(num * 5) + 1;

// console.log(num);

// If you want to generate random numbers just between 41 to 45

let num = Math.random(); //

num = Math.floor(num * 5) + 41;

console.log(num);
