// String chaining method is that we are performing 2 diiferent operations on a string in a same time

let str = "               Owais Nadeem             ";

// console.log(str);
// console.log(str.trim());

// console.log(str.toUpperCase());

// Now If I want to perform both operations at a time , then:

console.log(str.trim().toUpperCase());
