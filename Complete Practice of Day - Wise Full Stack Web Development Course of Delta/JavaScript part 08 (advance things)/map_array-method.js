// Map method of array 

// map method of array is used to perform the operations on the single array index

let arr = [45 , 32 , 12 , 94 , 65 , 42 , 10 , 5 ]

// let answer = arr.map((ind) => { return Math.sqrt(ind)})
let answer = arr.map((ind) => { return ind /2 }) // I found the divided answer of my array indexing elements through map method

console.log(answer)