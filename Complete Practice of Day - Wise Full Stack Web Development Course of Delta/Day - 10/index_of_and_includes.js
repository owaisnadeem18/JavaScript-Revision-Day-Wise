// Now, we have array methods as index of and includes methods

let arr = ["Owais", 456, true, false, "900"];

// IndexOf:
// It returns the index of something.

console.log(arr.indexOf("owais")); // it will return -1 , because 450 is not even existing in it

// Includes :
// It search for a value of something.

console.log(arr.includes(456)); // It returns boolean true , if the value exists inside it. Otherwise it returns false
